command:
  - uvicorn
  - app.backend.app:app
  - --host
  - 0.0.0.0
  - --port
  - "8000"

env:
  - name: DATABRICKS_SQL_WAREHOUSE_ID
    value: a62624c51dced859
  - name: APEX_CATALOG
    value: apex_fresh

resources:
  - name: apex-fresh-db
    type: lakebase_database

